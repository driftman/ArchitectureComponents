<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.driftman.fuckingandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity"></activity>
        <activity android:name=".ui.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.users.UsersActivity"></activity>

        <!-- Services -->
        <service android:name=".customlog.BSCCLogService">
            <meta-data android:name="url" android:value="http://api.log.bscc.com"/>
            <meta-data android:name="method" android:value="post"/>
            <meta-data android:name="worker_interval_ms" android:value="5000"/>
        </service>
    </application>

</manifest>